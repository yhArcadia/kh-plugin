<!--
 * @Author: 渔火Arcadia  https://github.com/yhArcadia
 * @Date: 2026-08-06 19:58:55
 * @LastEditors: 渔火Arcadia
 * @LastEditTime: 2026-09-22 18:14:10
 * @FilePath: /kh-plugin/CHANGELOG.md
 * @Description: 更新日志
 * 
 * Copyright (c) 2026 by 渔火Arcadia 1761869682@qq.com, All Rights Reserved. 
-->
# Changelog

# 2.1.7

* 字体统一为手机QQ默认字体、头衔样式跟随至手Q最新版样式。

# 2.1.6

* 新增`最亲群友`：在当前群所在**互通组**中查询与你**共同群**最多的群友

# 2.1.0 ~ 2.1.5

* **头像存储架构升级**：对头像文件的存存取链路进行了重构，实现**跨群去重**、**闲置头像清理**，实现更少的磁盘空间占用。并对非数字uid做兼容处理。
* `#kh统计`重写，现在会从多维度展示更多信息。
* 修复：框架缓存数据导致**批量**更新群员信息（定时更新、手动`#更新群员信息`）时录入缓存的旧昵称or群名片，配合实时检测，产生**一新一旧重复交替**的冗余记录。
* 优化：锅巴页面的**定时任务**配置项，由**手动填写cron表达式文本**改为交互式**cron生成器**，方便用户配置。
* 新特性：当kh的**查询者**和**被查询者**有本群以外的共同群，产出的图片中会有一条”**你们有x个共同群**“的提示（仅限已配置过的互通群范围内）。
* 新功能：**退群通报**。当检测到群友退群时，会在对应群通报提醒，并附带退群前的历史身份记录图片。 需要配置启用范围。
* 增加配置项：**渲染精度**。现在可按需增加渲染精度以提高出图清晰度、或降低精度换取速度。
* 排行榜类指令新增了一个共享的10秒cd。

# 2.0.x

* 2.0版本支持无感继承旧版已有的数据和配置；增加了对锅巴的支持；
* 新增指令：`#加群大王`、`#查看头像`、`#头像时间`、`#kh统计`、`#猜群友`、`#kh帮助`、`#kh更新`、`#kh版本`；
* 排行榜新增特定人员提到开头的机制；优化了`#老资历`、`#小资历`：额外展示**最后发言时间**；
* 新增可配置项：**历史身份倒序显示**、自定义触发推送的**变更类型**、推送规则分群配置；
* 配置文件修改即时生效；**实时监测**可监听Bot所处的全部群；

# 2.0.1 ~ 2.0.13

* `#头像时间`功能从文本形式改为更直观的图片形式。
* 新增`#加群大王`：取当前群所在的**互通组**并集，依据群友加入这些**互通组**中的群的数量为当前群的群员做排名。
* 增加`#查看头像`指令，等效于`#头像时间`。在该指令的卡片下方增加展示额外最多8个历史头像。
* 增加`#kh帮助`指令：查看插件指令列表与使用说明。
* 为全部指令统一了可选的`#`号前缀。
* **实时监测**方式改为Bot.on监听message.group，现在对**开启了仅at触发**和**开启了触发前缀**的群也会生效，能够及时更新其群员信息了。
* 新增`#kh统计`：查看kh-plugin的硬盘占用情况。
* 补充了`#kh帮助`、`#kh更新`、`#kh版本`此前遗漏的**潜水群判定**。
* 排行榜改为将**特定人员**（默认触发者，如果at了人则是被at的人）提到开头，以方便快速看到排名。
* 为 `#老资历` 和 `#小资历` 增加了**最近发言时间**标记，可以直观看到群友**加群时长**和**已潜水时长**。
* **新增配置项**：**历史身份倒序显示**，可用于设置是否倒序显示身份卡片，使最新记录在前，方便查看。
* 新增`#猜群友`：娱乐功能，随机发群友头像一角，猜对应群友。玩法见`#kh帮助`。
* **新增配置项**：**推送功能**支持自定义触发推送的**变更类型**（变更类型包含**头像**、**昵称**、**头衔**、**群名片**、**群权限**变更，默认全部开启），并支持**按群聊**分别配置。
* 优化：`#猜群友`将不再抽中已退群的成员。
* **体验优化**：修改配置文件**即时生效**，不再需要重启Bot。

# 2.0.0

* 随着1.x代码量增加，单文件已不便于维护，遂改为插件包模式。
* 兼容1.x已有的数据和配置，老用户**不需要手动迁移**，插件初次载入时自动处理合并。
* 载入时会自动处理1.x旧版js插件，不需要用户手动删除。
* 增加了对**Guoba**的支持。推荐使用Guoba进行配置。